name: child
procedures:
  child:
    description: E2E child — echoes its input back
    parameters:
      tag:
        type: string
    body:
    - id: echo
      value: { format: "child:${tag}", with: { tag: { ref: tag } } }
    result:
      tag_out: { ref: echo }
