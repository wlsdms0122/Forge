name: parent
procedures:
  parent:
    description: Cross-run dispatch — parent spawns a child run through the policy gate
    body:
    - id: fan
      dispatch:
        name: child
        inputs:
          tag: from-dispatch
        timeout: 60
    result:
      child: { ref: fan }
