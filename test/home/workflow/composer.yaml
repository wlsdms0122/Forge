name: composer
procedures:
  composer:
    description: Dynamic composition — data becomes steps at run time
    body:
    - id: made
      dynamic:
        compose:
        - value:
            id: fabricated
            value: composed-ok
        result: { ref: fabricated }
    result:
      result: { ref: made }
