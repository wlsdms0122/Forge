description: Dynamic composition — data becomes steps at run time
steps:
- id: made
  dynamic:
    compose:
    - value:
        id: fabricated
        value: composed-ok
    output: { ref: fabricated }
outputs:
  result: { ref: made }
